name: docker_build_test

on:
  pull_request:
  push:
    branches:
      - main

# cancel running jobs if there's a newer push
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

# Minimal permissions for security
permissions:
  contents: read

jobs:
  docker-build-rocky8:
    runs-on: ubuntu-latest
    env:
      # Override docker run options for non-interactive CI
      # Papiex Makefile default is "--rm -it", but -it causes "not a TTY" error
      # Keep --rm for cleanup, remove -it for non-interactive mode
      DOCKER_RUN_OPTS: "--rm"
      OS_TARGET: "rocky-8"
      # Values from giovtorres/slurm-docker-cluster tag 0.9.0 .env file
      # docker build does not read .env files (only docker-compose does),
      # so we define these here and pass SLURM_TAG as --build-arg
      SLURM_TAG: "slurm-25-05-3-1"
      IMAGE_TAG: "25.05.3"

    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
        with:
          # Use docker driver to store images in local daemon
          # (docker-container driver requires --load and may not propagate errors)
          driver: docker

      - name: Build slurm-cluster base image
        run: |
          # The rocky-8-epmt-test-release Dockerfile expects a slurm-cluster image.
          # Build it from giovtorres/slurm-docker-cluster tag 0.9.0 (Rocky Linux 8 based).
          # Pass SLURM_TAG as --build-arg since docker build does not read .env files.
          set -e
          git clone --branch 0.9.0 --depth 1 https://github.com/giovtorres/slurm-docker-cluster.git /tmp/slurm-docker-cluster
          docker build --progress=plain --build-arg SLURM_TAG=${SLURM_TAG} -t slurm-cluster:${IMAGE_TAG} /tmp/slurm-docker-cluster

      - name: Verify slurm-cluster image exists
        run: |
          if ! docker image inspect slurm-cluster:${IMAGE_TAG} > /dev/null 2>&1; then
            echo "::error::Docker image 'slurm-cluster:${IMAGE_TAG}' was not built."
            exit 1
          fi
          echo "Image 'slurm-cluster:${IMAGE_TAG}' exists."

      - name: Clean all
        run: make --debug OS_TARGET=${OS_TARGET} clean-all

      - name: Build epmt-dash
        run: make --debug OS_TARGET=${OS_TARGET} epmt-dash

      - name: Build papiex-dist
        run: make --debug OS_TARGET=${OS_TARGET} DOCKER_RUN_OPTS="${DOCKER_RUN_OPTS}" papiex-dist

      - name: Build docker-dist
        run: make --debug OS_TARGET=${OS_TARGET} DOCKER_RUN_OPTS="${DOCKER_RUN_OPTS}" docker-dist

      - name: Build epmt-full-release
        run: make --debug OS_TARGET=${OS_TARGET} DOCKER_RUN_OPTS="${DOCKER_RUN_OPTS}" epmt-full-release

      - name: Build check-release image
        run: make --debug OS_TARGET=${OS_TARGET} DOCKER_RUN_OPTS="${DOCKER_RUN_OPTS}" build-check-release

      - name: Verify check-release image exists
        run: |
          # Extract EPMT_VERSION using the same sed logic as the Makefile (line 44)
          EPMT_VERSION=$(sed -n '/_version = /p' src/epmt/epmtlib.py | sed 's/ //g; s/,/./g; s/.*(\(.*\))/\1/')
          IMAGE="${OS_TARGET}-epmt-test-release:${EPMT_VERSION}"
          echo "Checking for Docker image: ${IMAGE}"
          if ! docker image inspect "${IMAGE}" > /dev/null 2>&1; then
            echo "::error::Docker image '${IMAGE}' was not built. The build-check-release step likely failed silently."
            exit 1
          fi
          echo "Image '${IMAGE}' exists."

      - name: Set up check-release test infrastructure
        run: |
          # Extract EPMT_VERSION using the same sed logic as the Makefile
          EPMT_VERSION=$(sed -n '/_version = /p' src/epmt/epmtlib.py | sed 's/ //g; s/,/./g; s/.*(\(.*\))/\1/')
          CONTAINER_NAME="${OS_TARGET}-epmt-${EPMT_VERSION}-test-release"
          IMAGE_NAME="${OS_TARGET}-epmt-test-release:${EPMT_VERSION}"
          echo "EPMT_VERSION=${EPMT_VERSION}" >> $GITHUB_ENV
          echo "CONTAINER_NAME=${CONTAINER_NAME}" >> $GITHUB_ENV

          # Clean up any previous resources
          if docker ps --filter "name=^postgres-test$" --format '{{.Names}}' | grep -q .; then docker stop postgres-test; fi
          if docker network ls --filter "name=^epmt-test-net$" --format '{{.Name}}' | grep -q .; then docker network rm -f epmt-test-net; fi

          # Create docker network
          docker network create epmt-test-net

          # Start postgres container
          docker run -d --rm --name postgres-test --network epmt-test-net --privileged \
            -e POSTGRES_USER=postgres \
            -e POSTGRES_PASSWORD=example \
            -e POSTGRES_DB=EPMT-TEST postgres:latest

          # Remove any leftover test container (sleep allows Docker to finish cleanup)
          if docker container ls -a --filter "name=^${CONTAINER_NAME}$" --format '{{.Names}}' | grep -q .; then
            docker container rm ${CONTAINER_NAME} || true
            sleep 2
          fi

          # Start test container in detached mode
          docker run -d --name ${CONTAINER_NAME} \
            --network epmt-test-net \
            --privileged \
            -h slurmctl ${IMAGE_NAME} \
            bash -c 'echo 2 > /proc/sys/kernel/perf_event_paranoid && sleep infinity'

      - name: epmt version
        run: docker exec ${{ env.CONTAINER_NAME }} epmt -vv -V

      - name: epmt check
        continue-on-error: true
        run: docker exec ${{ env.CONTAINER_NAME }} epmt -vv check

      - name: "unit-test: test_anysh"
        continue-on-error: true
        run: docker exec ${{ env.CONTAINER_NAME }} bash -c 'TZ=UTC python3 -m unittest -v epmt.test.test_anysh'

      - name: "unit-test: test_cmds"
        continue-on-error: true
        run: docker exec ${{ env.CONTAINER_NAME }} bash -c 'TZ=UTC python3 -m unittest -v epmt.test.test_cmds'

      - name: "unit-test: test_db_migration"
        continue-on-error: true
        run: docker exec ${{ env.CONTAINER_NAME }} bash -c 'TZ=UTC python3 -m unittest -v epmt.test.test_db_migration'

      - name: "unit-test: test_db_schema"
        continue-on-error: true
        run: docker exec ${{ env.CONTAINER_NAME }} bash -c 'TZ=UTC python3 -m unittest -v epmt.test.test_db_schema'

      - name: "unit-test: test_explore"
        continue-on-error: true
        run: docker exec ${{ env.CONTAINER_NAME }} bash -c 'TZ=UTC python3 -m unittest -v epmt.test.test_explore'

      - name: "unit-test: test_lib"
        continue-on-error: true
        run: docker exec ${{ env.CONTAINER_NAME }} bash -c 'TZ=UTC python3 -m unittest -v epmt.test.test_lib'

      - name: "unit-test: test_outliers"
        continue-on-error: true
        run: docker exec ${{ env.CONTAINER_NAME }} bash -c 'TZ=UTC python3 -m unittest -v epmt.test.test_outliers'

      - name: "unit-test: test_query"
        continue-on-error: true
        run: docker exec ${{ env.CONTAINER_NAME }} bash -c 'TZ=UTC python3 -m unittest -v epmt.test.test_query'

      - name: "unit-test: test_run"
        continue-on-error: true
        run: docker exec ${{ env.CONTAINER_NAME }} bash -c 'TZ=UTC python3 -m unittest -v epmt.test.test_run'

      - name: "unit-test: test_settings"
        continue-on-error: true
        run: docker exec ${{ env.CONTAINER_NAME }} bash -c 'TZ=UTC python3 -m unittest -v epmt.test.test_settings'

      - name: "unit-test: test_shell"
        continue-on-error: true
        run: docker exec ${{ env.CONTAINER_NAME }} bash -c 'TZ=UTC python3 -m unittest -v epmt.test.test_shell'

      - name: "unit-test: test_stat"
        continue-on-error: true
        run: docker exec ${{ env.CONTAINER_NAME }} bash -c 'TZ=UTC python3 -m unittest -v epmt.test.test_stat'

      - name: "unit-test: test_submit"
        continue-on-error: true
        run: docker exec ${{ env.CONTAINER_NAME }} bash -c 'TZ=UTC python3 -m unittest -v epmt.test.test_submit'

      - name: "integration: 001-basic"
        continue-on-error: true
        run: docker exec ${{ env.CONTAINER_NAME }} epmt -v integration 001-basic.bats

      - name: "integration: 002-escape"
        continue-on-error: true
        run: docker exec ${{ env.CONTAINER_NAME }} epmt -v integration 002-escape.bats

      - name: "integration: 005-concat"
        continue-on-error: true
        run: docker exec ${{ env.CONTAINER_NAME }} epmt -v integration 005-concat.bats

      - name: "integration: 010-daemon"
        continue-on-error: true
        run: docker exec ${{ env.CONTAINER_NAME }} epmt -v integration 010-daemon.bats

      - name: "integration: 011-shell"
        continue-on-error: true
        run: docker exec ${{ env.CONTAINER_NAME }} epmt -v integration 011-shell.bats

      - name: "integration: 015-annotate"
        continue-on-error: true
        run: docker exec ${{ env.CONTAINER_NAME }} epmt -v integration 015-annotate.bats

      - name: "integration: 020-explore"
        continue-on-error: true
        run: docker exec ${{ env.CONTAINER_NAME }} epmt -v integration 020-explore.bats

      - name: "integration: 025-slurm"
        continue-on-error: true
        run: docker exec ${{ env.CONTAINER_NAME }} epmt -v integration 025-slurm.bats

      - name: "integration: 030-collate-tsv"
        continue-on-error: true
        run: docker exec ${{ env.CONTAINER_NAME }} epmt -v integration 030-collate-tsv.bats

      - name: "integration: 050-kernel-compile"
        continue-on-error: true
        run: docker exec ${{ env.CONTAINER_NAME }} epmt -v integration 050-kernel-compile.bats

      - name: Clean up test infrastructure
        if: always()
        run: |
          docker stop ${{ env.CONTAINER_NAME }} || true
          docker stop postgres-test || true
          docker network rm epmt-test-net || true
