#!/usr/bin/env python3
'''
Legacy entry-point script for the epmt package.

The canonical CLI logic now lives in epmt.cli.main().
This wrapper is kept for backward compatibility with existing
deployments that invoke ``scripts/epmt`` directly.
'''

from epmt.cli import main

if __name__ == "__main__":
    main()
